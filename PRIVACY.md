# 隐私说明

## 适用范围

本说明适用于知序 Windows 客户端及其配套的账户同步和每日格言服务。当前实现不包含遥测、广告 SDK 或行为分析服务。

## 数据处理边界

| 数据类别                                             | 处理位置                          | 处理目的                                 |
| ---------------------------------------------------- | --------------------------------- | ---------------------------------------- |
| 任务、分类、标签、日程、专注、生活事件、倒计时、备忘 | 本地 SQLite；已绑定账户的同步存储 | 核心工作区与跨设备同步                   |
| 支付宝和微信原始账单文件                             | 本机内存与临时导入流程            | 预览、解析和去重；原始文件不作为文件上传 |
| 导入后的结构化财务记录                               | 本地 SQLite；已绑定账户的同步存储 | 消费分析与跨设备同步                     |
| 邮箱、账户标识和认证会话                             | Supabase Auth；当前设备安全存储   | 注册、登录、邮箱验证和会话恢复           |
| 每日格言、收藏和不喜欢反馈                           | 本地 SQLite；已绑定账户的同步存储 | 内容展示、去重和偏好调整                 |
| 应用版本和更新请求                                   | GitHub Releases                   | 检查、下载和安装稳定版本                 |

本地数据库默认位于 `%LOCALAPPDATA%\Zhixu\Data\zhixu.sqlite`。旧版迁移备份和首次同步备份分别位于 `%LOCALAPPDATA%\Zhixu\MigrationBackups` 与 `%LOCALAPPDATA%\Zhixu\SyncBackups`。

## 账户与同步

账户认证和同步服务由 Supabase 提供。当前同步实体包括任务分类、标签、任务、标签关联、日程区块、专注记录、生活事件、倒计时、财务记录和每日格言。服务端使用行级安全策略按账户隔离数据。

首次账户绑定会在本地创建备份，再合并本地和云端数据。首次同步完成后，应用可在离线状态继续使用，并在网络恢复后提交待同步变更。

登录会话通过 Electron `safeStorage` 使用当前 Windows 用户的系统保护能力加密保存。设备标识为客户端生成的随机 UUID，不包含硬件序列号。

## 每日格言服务

每日格言可来自随应用提供的离线精选语料，也可由 AI 服务生成。AI 路径由 Supabase Edge Function 调用 DeepSeek API，发送内容包括：

- 当前本地日期；
- 最多 8 条已收藏格言；
- 最多 12 条不喜欢格言；
- 用于服务端去重的近期格言不会写入 AI 提示正文。

任务、备忘、专注记录、财务记录、邮箱、密码和原始账单文件不会加入每日格言的 AI 请求。第三方服务的数据处理同时受 Supabase 和 DeepSeek 的服务条款与隐私政策约束。

## 更新与外部链接

打包版本通过 GitHub Releases 检查更新并下载发布资产。应用只允许将受支持的 GitHub 和 Supabase HTTPS 链接交由系统浏览器打开。开发构建不执行自动更新检查。

## 数据保留与清理

- 退出账户会清除当前设备保存的认证会话，但不会自动删除本地业务数据库或云端业务数据。
- 卸载应用不会承诺删除应用数据、迁移备份或同步备份。
- 本地数据清理需在应用完全退出后删除上述知序数据目录；该操作不可恢复。
- 云端账户或同步数据删除需通过仓库维护者提供的私密联系渠道提出，不应在公开 Issue 中提交邮箱、账单或其他个人数据。

## 隐私问题

隐私缺陷或可能导致数据泄露的问题应按 [安全策略](SECURITY.md) 使用 GitHub Private vulnerability reporting 提交。一般隐私说明问题可创建公开 Issue，但不得附带真实账户信息、数据库、日志、账单或访问凭据。
